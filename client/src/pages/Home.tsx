import { getCurrentDrivers } from "@/services/api";
import { useState, useEffect } from "react";

function Home() {
  const [drivers, setDrivers] = useState([]);

  return <div>Home</div>;
}
export default Home;
